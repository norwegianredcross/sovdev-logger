# Environment Configuration for sovdev-logger

# =============================================================================
# IMPORTANT: DevContainer Networking
# =============================================================================
# When running inside the DevContainer, use 'host.docker.internal' to reach
# services on the host machine (Kubernetes cluster running via Rancher Desktop)
#
# When running directly on host (Mac/Windows/Linux), use '127.0.0.1' or 'localhost'
#
# Available options:
# - host.docker.internal (recommended for devcontainer - works on Mac/Windows/Linux)
# - 172.17.0.1 (Docker bridge gateway - may vary by setup)
# - 127.0.0.1 (when running on host directly)
# =============================================================================

# Kubernetes host - automatically detects environment
# Default: host.docker.internal (for devcontainer)
# Override: Set to 127.0.0.1 when running on host
KUBE_HOST=host.docker.internal

# OpenTelemetry Collector endpoint (through Traefik ingress)
# Requires Host header: 'otel.localhost'
OTEL_EXPORTER_OTLP_LOGS_ENDPOINT=http://${KUBE_HOST}/v1/logs
OTEL_EXPORTER_OTLP_METRICS_ENDPOINT=http://${KUBE_HOST}/v1/metrics
OTEL_EXPORTER_OTLP_TRACES_ENDPOINT=http://${KUBE_HOST}/v1/traces

# Required HTTP headers for Traefik routing
OTEL_EXPORTER_OTLP_HEADERS={"Host":"otel.localhost"}

# System identification
SYSTEM_ID=sovdev-logger-typescript

# Log configuration
LOG_LEVEL=info
LOG_TO_CONSOLE=true
